<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Flashcards.Mobile.Authentication"
             x:Class="Flashcards.Mobile.Authentication.RegistrationPage"
             x:DataType="{x:Type local:RegistrationPageViewModel}"
             Shell.PresentationMode="ModalAnimated">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Registration Page!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <Button Text="Close" Command="{Binding CloseCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>